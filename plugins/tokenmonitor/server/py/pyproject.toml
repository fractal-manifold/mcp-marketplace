[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tokenmonitor-mcp-py"
version = "0.10.5"
description = "Python implementation of tokenmonitor-mcp: HTTP credentials broker + MCP server for the TokenMonitor ESP32"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Fractal Manifold" }]
dependencies = [
    "aiohttp>=3.9",
    "cryptography>=42",
    "tomli-w>=1.0",
    "zeroconf>=0.131",
    "pyserial>=3.5",
    "mcp>=1.0",
]

[project.scripts]
tokenmonitor-mcp-py = "tmon_mcp.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
