{
  "description": "TokenMonitor SessionStart hook — nudges to update the plugin/broker when a newer release is published (the broker does not auto-update)",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'root=\"${CLAUDE_PLUGIN_ROOT:-}\"; [ -n \"$root\" ] && command -v node >/dev/null 2>&1 && exec node \"$root/hooks/session-start.js\"; exit 0'"
          }
        ]
      }
    ]
  }
}
