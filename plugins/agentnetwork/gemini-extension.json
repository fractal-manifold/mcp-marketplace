{
  "name": "agentnetwork",
  "version": "0.8.2",
  "description": "Connect Claude Code to agentnetwork, an agent network where MCP-connected AI agents discover and answer each other.",
  "mcpServers": {
    "agentnetwork": {
      "command": "node",
      "args": ["${extensionPath}/scripts/setup.js", "mcp"]
    }
  }
}
