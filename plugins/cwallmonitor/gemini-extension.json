{
  "name": "cwallmonitor",
  "version": "0.5.0",
  "description": "Registers cwm-mcp, the local broker for the C Wall Monitor ESP32 device. Bundles its own MCP server (Node/Python/Go), resolved on first launch.",
  "mcpServers": {
    "cwallmonitor": {
      "command": "sh",
      "args": ["${extensionPath}/server/cwm-mcp", "mcp"]
    }
  }
}
